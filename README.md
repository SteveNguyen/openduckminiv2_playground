Install `uv`:
`curl -LsSf https://astral.sh/uv/install.sh | sh`

`uv run test_install.py` to check your installation
`uv run test_train` to run a training

To create a venv:
`uv venv`
`source .venv/bin/activate`
